export interface RouteStatus{
    dashboard:boolean,
    classes:boolean
    classrooms:boolean,
    teachers:boolean,
    subjects:boolean,
    students:boolean
}
export interface ClassesRouteStatus{
    allClasses:boolean
}